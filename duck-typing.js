//TODO: Duck Typing, es identificar de donde proviene la instancia, se usa instanceof
//identificar la procedencia de nuestros recursos, variables
//como identificar el elemento que necesitamos en el codigo, de donde viene, de que prototype viene
//no basta con que tenga la misma estructura, sino la procedencia, xq la estructura se puede harcodear desde fuera
//y no desde un flujo del codigo, que cumpla el flujo del negocio o proceso interno

//El duck typing es la forma de progamar donde identificamos a nuestros elementos dependiendo de los métodos y atributos que tengan por dentro.

//PORQUE ESTA EN POO?, porque debemos saber de que instancia viene, ya que si un hacker desde afuera crea una estructura de datos
//y setea esto en alguna api, podemos ser vulnerables, sin embargo si antes de ejecutar el api, se valida que la estructura de datos
//derive de una instancia de modo que esta esta en el flujo del proceso de negocio en el codigo, habra mas seguridad

//instanceof